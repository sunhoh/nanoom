import React from 'react';
import styled from 'styled-components';

const BoardList = () => {
  return <Container>borderlist</Container>;
};

export default BoardList;

const Container = styled.div`
  width: 100vw;
  height: 100vh;
  text-align: center;
`;
